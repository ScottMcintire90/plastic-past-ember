{{#link-to 'item' item.id}}
  <div class="container tile-container">
    <ul class="img-list">
      <li>
        <img class="tile-images" src='{{item.image}}'>
        <span class="text-tile"><span>{{item.name}} ${{item.price}}</span></span>
      </li>
    </ul>
  </div>
{{/link-to}}
